# 更新日志

按版本倒序。**新版本的功能说明看 [`README.md`](README.md)**，这里只记「每一版改了什么」。
工程上的取舍与踩坑记录在 [`docs/dev-notes.md`](docs/dev-notes.md)。

类型标记：`feature` 新功能 · `ui` 界面 · `fix` 修 bug · `perf` 性能 · `docs` 文档 · `breaking` 需要用户动手

---

## v1.9.0 —— 2026-09-21

`feature` `ui` `fix`

侧边栏那一页接着收口：传输不再模态阻塞，主题与仓库都改成卡片，设置页重排。
左栏两栏按用户反馈改名 ——「主题同步」→「主题」、「仓库与归档」→「仓库」。

- `feature` **传输不再模态阻塞**：归档 / 取回 / 卸载 / 主题同步改成一条**串行队列**
  在后台跑（`Services/TransferQueue.cs`），页面底部常驻一条聚合进度条，左栏新增
  「任务」一栏可以逐条看进度与失败原因，也能整体取消或清除已结束的。原来走的是
  Playnite 的 `ActivateGlobalProgress` —— 模态对话框，跑着的时候整个 Playnite 被按住，
  大游戏一传几十分钟什么也干不了。队列刻意**串行**：NAS 的写入侧才是瓶颈
  （实测下载 88 MB/s、上传只有 20~40 MB/s），两条并发只会互相抢带宽、
  还让两条进度条都不准
- `feature` 主题页改成**卡片勾选式**：本机与 NAS 上的主题各一张卡（带体积与状态），
  左上角「全选」；勾了哪几个就只搬哪几个，没勾的连扫描结果都不参与比对
- `fix` **修掉「同一个游戏在卡片墙上出现两次」**：老归档（v1.8 之前）没存游戏 ID，
  本地那份又已经卸载（安装目录没了 → 推不出 slug），前三档对账全落空 —— 于是一个显示
  「未上传」、另一个又作为孤儿再出现一次。命中键从三级扩到**五级**（新增
  「库内标识 == 条目 Id」与「归一化名字」），并把本机与远端并成同一张卡
- `ui` **「仓库与归档」改名「仓库」**：本机游戏与只在 NAS 上的条目铺在**同一面墙**上，
  标出「已归档 / 未归档 / 只在 NAS」与各自体积；未归档的可直接归档，只在 NAS 的
  可以**取回本机**（原来那一段「孤儿条目」只能删、不能取回）
- `ui` **「主题同步」改名「主题」**；左栏新增「任务」一栏
- `ui` 状态点从整页右上角挪到左栏「仓库管家」标题右侧，点它立刻重测（探测期间转圈）；
  概览页原来那个「刷新」按钮删掉 —— 一个动作只留一个入口
- `ui` 设置页重排成「连接 NAS / 传输 / 自动化 / 云存档」四组，每组一张带左侧色条的卡片；
  输入框的「当前值」提示改成**水印**（框里没东西时显示原值，一输入就藏），
  不再挤在右边跟输入内容抢地方
- `fix` 设置页嵌进侧边栏时**滚轮乱滚**：它自带一层 `ScrollViewer`，外面还有一层，
  内层先把滚轮事件吃掉 —— 现在嵌入时不再建内层滚动区
- `fix` 保存设置改成**回报真实结果**（`SaveSettingsNow` 返回成败），存完重新快照一次输入框 ——
  原来被夹到范围外的值在界面上看不出被改了，看着像「没保存」
- `fix` 浅色档的次要文字提深（`#7C7F93` → `#5A5E72`，对比度约 3.9:1 → 5.4:1）
- `docs` 侧边栏整页的版面示意图、主题 / 仓库 / 任务的说明同步进 README 与使用说明

自检从 303 项加到 **345 项**：新增五级命中键、名字归一化、主题归并、传输队列四组，
另补了一条「左栏每个分栏都必须有导航项」的断言 —— v1.9 开发中真踩过一次
（任务页的段落注册了、`AddSection` 却漏了，用户只能从底部进度条的「详情」绕进去）。

---

## v1.8.1 —— 2026-09-21

`ui` `fix` `feature`

侧边栏那一页重新做了版面，并把「删归档」从独占窗口挪进页面里。
（1.8.0 只发到内部试用就发现打不开，没作为正式版发出；本版是 1.8 的第一个公开发布。）

- `fix` 修掉一个会让 **Playnite 打不开**的崩溃：概览页的统计卡片把同一个 `UIElement`
  先赋给 `Border.Child`、又 `Add` 进 `Grid` —— WPF 的逻辑树不允许一个元素有两个父节点，
  侧边栏一构建就抛 `InvalidOperationException`。自检里补了「在 STA 线程上把整页
  `VaultPanelView` 真建一遍」这一组，修之前先把旧代码塞回去验证过它确实会红
- `ui` 侧边栏图标改成**实心扁平风**（原来是细线条描边，和 Playnite 自带按钮不是一路）
- `ui` 整页换成一套**语义配色令牌**：明 / 暗 / 跟随 Playnite 三档，可在设置里手动指定。
  原来「自动」判错时用户没有退路；现在配错主题也能自己压回看得清的档位
- `ui` **顶栏整个去掉**：标题在左边栏已有；原来贴在右上角的「已连接 / 刷新」正好压在
  Playnite 的最小化 / 最大化 / 关闭按钮上 —— 点刷新会顺手关掉 Playnite。
  改成一个不占版面的小圆点（绿=连得上、黄=暂时连不上、红=配置或凭据不对、灰=没配 WebDAV），
  点它重新探测
- `ui` 左边栏「Playnite Vault @版本号」直接显示当前版本，原来那页「关于 / 依赖」整页删掉
- `ui` 概览页：刷新按钮收进页内，指标块 + 环形 / 条形 / 堆叠条三张图；
  原来那片「常用操作」按钮散回各自所属的页面
- `ui` 设置页重排：分组 + 每个字段标出**当前值**、ⓘ 悬停说明、输入框带占位提示；
  「检查插件更新」「打开日志文件」收进这里。内嵌页补上保存 / 放弃按钮
  （Playnite 只在它自己的设置对话框里给这两个按钮，嵌进来的这一页原来改什么都存不下）
- `feature` **仓库与归档页改成卡片墙**：本机 Playnite 库里每个游戏一张卡片（封面 + 已上传 /
  未上传），可筛选、可按名字过滤。判定「传过没有」按 Playnite 游戏 ID → 库内 ID（`Game.GameId`，
  Steam 就是 appid）→ 安装目录名 slug 三级，命中依据写在卡片提示里
- `feature` **删除改成页内直删**（原来必须单开一个仓库管理窗口）：卡片上和孤儿条目上都能删，
  口令闸门仍然只有一道（`VaultPlugin.DeleteRepositoryApp`），自检里有 IL 级断言盯着
  「`VaultService.RemoveApp` 的调用方只有这一处」。仓库管理窗口保留，只用于改管理口令
- `feature` 卡片上未上传的游戏可以直接「归档到 NAS」，走的是右键菜单同一条链路
- `feature` 仓库里有、本地库里已经没有的条目（换过机器 / 游戏已从库里删掉）单独列出来，
  也能直接删 —— 这些条目本来只能在管理窗口里处理
- `fix` 归档时把来源记录的 `Game.Id`（GUID）与 `Game.GameId`（库内标识）写进索引，
  这样目录改名、手工改显示名之后仍然认得出来「这条传过」
- `fix` 状态徽章的字色按底色亮度自动挑（实心绿 / 橙块上的字），
  浅色档下原来的深字压在红色块上只有 3.0:1，等于看不清；自检对 5 种语义色 × 明暗两档逐个断言 ≥ 4.5

实测结论（免得下次再撞）：Playnite SDK 6.13 里 **`Game` 上没有 `DatabaseId` 这个成员**，
`IGameDatabase` 也没有按 int 取游戏的入口 —— 最初想用的「GUID + 数据库自增 id」第二把钥匙拿不到，
改用 `Game.GameId`（库内标识）。它在「游戏删掉再重新导入」这一档上比 GUID 更稳
（重导入会分配新 GUID），在「跨机器」这一档上两者等价。

---

## v1.7.0 —— 2026-09-21

`feature` `ui` `fix`

**云存档**：把游戏存档也存进 NAS（Playnite 自带的云存档只有界面与数据模型，没有云存储实现）。

- `feature` 内容寻址存储（`objects/xx/<sha1>`）+ 快照 + 分支；同一份内容跨快照、跨分支只占一份
- `feature` 路径两份合并：Playnite 的 `Game.SavePaths` 优先，再合插件自己的表，按游戏标题归并
- `feature` 三档嗅探：常见位置启发式 / 本次会话差分（mtime+size 指纹 + 向上爬父目录）/ PCGamingWiki；**只产候选，绝不自动写入**
- `feature` 触发时机三档：手动 / 停止游戏后自动上传 / 停止后上传＋启动时问
- `feature` 保留策略按分支独立：每个分支最多留几份（`0` = 无限）、固定过的永不删、最新一份必留
- `feature` 命令行入口 `VaultPack saves --action list|branches|upload|download|delete|prune|sniff`
- `ui` 侧边栏新增「存档管理」入口；插件设置里新增云存档一节
- `fix` 解包器：**托盘 / 隐藏状态下的 Playnite 关不掉** —— 原来只给「可见」窗口发 `WM_CLOSE`，
  而托盘时主窗口 `IsWindowVisible` 就是 `false`，于是「0 个窗口 → 等满 60 秒 → 请你手动退出」。
  现在可见优先、全不可见时挑隐藏主窗口，并在等待期间重试补发
- `fix` 解包器：**内置插件包按文件名排序取第一个** —— 字符串排序下 1.6.0 与 1.7.0 并存会挑中旧包，
  且将来 `1.10.0` 会被排在 `1.9.0` 前面。改成按版本号比大小

实测结论（免得下次再撞）：Playnite SDK 6.13 的 `AutoAdaptivePathHelper` 是**恒等函数** ——
`{WinLocalAppData}`、`%LOCALAPPDATA%` 等写法一律原样返回、`IsAdaptivePath` 恒 `false`，
所以没有官方 token 契约可对齐，插件自持一张表。

自检：C# 239 项、Python 109 项、主题抓取 25 项，全部退出码 0。

---

## v1.6.0 —— 2026-09-20

`feature` `fix` `ui` `breaking`

- `breaking` 插件改名 `Playnite-Vault`（去掉临时名里那串 GUID）。插件包文件名从
  `VaultDemo-*.zip` 变成 `PlayniteVault-*.zip` —— **≤1.5.0 的自动更新只会找老名字，搜不到这一版**，
  需要用 1.6.0 的解包器装一次；之后自动更新恢复正常
- `feature` 插件数据目录**自动迁移**（`settings.json` / 本地索引 / 缓存索引 / 运行状态 /
  `meta-cache/`）到新目录，WebDAV 配置与账号不用重配，旧目录原地保留不删
- `feature` 解包器新增「安装插件到 Playnite…」：自动定位 Playnite → 选插件包来源
  （内置离线包 / GitHub / Gitee）→ **优雅关闭** Playnite（等同点 ×，不强杀）→ 写入
  `Extensions\Playnite-Vault\` → 按原样重启
- `fix` **打包排除清单误伤 `concurrent`**，导致 exe 一导入核心模块就 `ModuleNotFoundError`
  —— 源码跑得通、冻结后必炸。已修，并加了「打完 exe 立刻跑一次冻结自检」的强制关卡
- `fix` exe 的命令行参数以前不生效（入口只认 `--selftest`，其它参数一律弹窗）
- `fix` 解包器只认 `Playnite.exe`，而实际进程是 `Playnite.DesktopApp.exe` /
  `Playnite.FullscreenApp.exe`（管理员模式还有 `.Admin`）—— 这个 bug 会让它在 Playnite 开着时
  写本地索引，写进去的内容随后被 Playnite 覆盖掉
- `ui` 旧版本备份挪到 `%APPDATA%\VaultUnpacker\plugin-backup\<时间戳>\`，不再堆在 `Extensions\`

---

## v1.5.0 —— 2026-09-20

`feature` `perf`

- `feature` **打开 Playnite 自动检查并安装插件更新**：启动 12 秒后并发探 GitHub 与 Gitee 的
  `releases/latest`，比版本号（`v` 前缀、预发布、`1.10 > 1.9` 都按段比）
- `feature` **国内 / 国外自动换源**：两个源并发探测、按实测延迟排序；下载途中速度掉到地板
  （默认 40 KB/s）以下就换另一个源
- `feature` 所有源都低于地板时，用主源再跑一次**不设地板**的兜底下载 —— 慢网络照样能装完
- `feature` 设置里可开自动刷新远端库，间隔 5 分钟 ~ 24 小时自选
- `perf` 运行游戏时自动暂停刷新，游戏结束后按已流逝时间补上
- `perf` 索引没变就不动本地库（指纹**刻意排除 `UpdatedAt`**，重新归档不会引发无意义的库重写）
- 新增 `tools/VaultSelfTest/`：本地假 GitHub / 假 Gitee，把上面每条都真跑一遍

---

## v1.4.0 —— 2026-09-19

`feature` `fix` `ui` `breaking`

- `breaking` 存储格式升级到 **v3 内容寻址区块**（旧归档读取端仍完全支持，重归档才需要）
- `fix` **大游戏传不上去**（速度从 20 MB/s 掉到 0 然后超时）：旧格式整文件独占一个分片，
  `res.pak` 1.93 GB 就变成一个 1.93 GB 的 PUT；且超时用的是建连超时，必定撞死。
  现在固定 32 MB 切块、一个文件可跨多块，超时拆成建连 / 停滞 / 应答三档，
  「停滞」才是「速度到 0」的正确判据，单块失败自动重试
- `fix` **装回去的目录名变成了 Playnite 里的中文显示名**（有些游戏在中文目录下起不来）：
  原因是目录名直接用了 `appId`，而 slug 生成用 `char.IsLetterOrDigit` —— 它对 CJK 返回 `true`。
  现在 `Id` 与文件夹名彻底分离，文件夹名取「上传前安装目录的最后一段」
- `feature` 游戏列表右键 → **管理仓库应用**（删除需管理口令，口令存在仓库根的 `vault-admin.json`）
- `perf` 下载区块并发 4 路；区块下完立刻解包并删临时文件（峰值临时占用只有「并发数 × 区块大小」）
- `ui` **401 精确诊断**：区分「密码填错」与「服务端认证后端坏了」—— 两者的 401 响应完全一样，
  光看状态码分不出来

---

## v1.3.0 —— 2026-09-18

`feature` `ui` `fix`

- `feature` 插件与解包器**统一版本号**
- `feature` 解包落盘的子目录名优先使用归档前的**原始安装目录名**，取不到才回退 `appId`
- `ui` 解包器配置简化为 **exe 同目录的 `config.json`**，首次启动自动生成
- `fix` 解包器列表里「输出父目录」标签与输入框重叠
- `fix` 加列后状态列写错位置，导致「已安装」显示到分片数那一列
